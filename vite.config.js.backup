import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';

export default defineConfig({
    plugins: [
        laravel({
            input: ['resources/css/app.css', 'resources/js/app.js'],
            refresh: true,
            // Exclude vendor directory from watch (optional)
            valetTls: false,
        }),
    ],
    server: {
        watch: {
            ignored: ['**/vendor/**'],
        },
    },
});
    // Add Font Awesome to input
    input: ['resources/css/app.css', 'resources/js/app.js', 'node_modules/@fortawesome/fontawesome-free/css/all.min.css'],
